// import db from '@/database/db';

// To seed the database
// async function createCustomer() {
//   customers.forEach(async (customerData) => {
//     await db.customer.create({
//       data: customerData,
//     });
//   });
// }
// createCustomer();

export default function Page() {
  return <p>Dashboard Page</p>;
}
